import { Renderer2 } from '@angular/core';
import { Router } from '@angular/router';
import { NgxGtag } from '../google-analytics.interface';
export declare class GoogleAnayticsService {
    private router;
    private config;
    private document;
    private platform;
    trackingId: string;
    renderer: Renderer2;
    constructor(router: Router, config: NgxGtag, document: any, platform: Object);
    startTracking(): void;
}
