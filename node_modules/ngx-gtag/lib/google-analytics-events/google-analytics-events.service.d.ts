import { NgxGtagEvent } from '../google-analytics.interface';
export declare class NgxGtagEventService {
    private platform;
    constructor(platform: Object);
    event(event: NgxGtagEvent): void;
}
